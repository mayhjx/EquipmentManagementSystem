@page
@model EquipmentManagementSystem.Pages.Calibrations.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Calibration[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Calibration[0].Result)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Calibration[0].Unit)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Calibration[0].Calibrator)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Calibration[0].instrument)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Calibration) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Result)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Unit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Calibrator)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.instrument.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
