@page
@model EquipmentManagementSystem.Pages.Management.Groups.EditModel

@{
    ViewData["Title"] = "编辑";
}

<div class="card">
    <div class="card-header">
        项目组
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-3">
                <form method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <input type="hidden" asp-for="Group.Id" />
                    <div class="form-group">
                        <label asp-for="Group.Name" class="control-label"></label>
                        <input asp-for="Group.Name" class="form-control" />
                        <span asp-validation-for="Group.Name" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="保存" class="btn btn-primary" />
                    </div>
                </form>
            </div>
        </div>

        <div>
            <a asp-page="./Index">返回</a>
        </div>
    </div>
</div>

@*<div class="card">
        <div class="card-header">
            检测项目
        </div>
    <div class="card-body">
        <div class="card-title">
            检测项目
            <a asp-page="../Projects/Create" asp-route-groupId="@Model.Group.Id">
                <input type="button" class="btn btn-outline-primary btn-sm" value="新建项目" style="margin-left: 10px;" />
            </a>
        </div>

        <table class="table text-nowrap">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Group.Projects.First().Name)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Group.Projects)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            <a asp-page="../Projects/Edit" asp-route-id="@item.Id">编辑</a> |
                            <a asp-page="../Projects/Delete" asp-route-id="@item.Id">删除</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <div>
            <a asp-page="./Index">返回</a>
        </div>
    </div>
    </div>
*@
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
